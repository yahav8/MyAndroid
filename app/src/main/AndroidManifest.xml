<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFinal">

        <activity
            android:name=".Start.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PostCode.PostFeed" android:exported="false" />
        <activity android:name=".PostCode.PostCreator" android:exported="false" />

        <activity android:name=".Start.AvatarPicker" android:exported="false" />
        <activity android:name=".Start.ForgotPassword" android:exported="false" />
        <activity android:name=".PreferredSettingsP" android:exported="false" />
        <activity android:name=".Start.AboutProgrammer" android:exported="false" />
        <activity android:name=".Start.AboutProject" android:exported="false" />
        <activity android:name=".Profile" android:exported="false" />
        <activity android:name=".Start.SignUp" android:exported="false" />
        <activity android:name=".Start.LogIn" android:exported="false" />
        <activity android:name=".Start.OpenPage" android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>